import { ComponentRendererArgs } from '../select-types';
declare const Clear: <T extends Record<string, any>>({ props, state, methods }: ComponentRendererArgs<T>) => JSX.Element;
export default Clear;
